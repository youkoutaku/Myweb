/*
var access =
	document.getElementById("myname");
//使用document对象中的getElementById的方法，获取id为code9的元素，把这个元素赋给access
var code = access.innerHTML;
//通过access中的innerHTML的方法，获取它的文本内容，赋给code
alert(code);
可行
*/
